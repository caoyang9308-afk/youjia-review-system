{
  "installCommand": "cd /workspace/projects && pnpm install --no-frozen-lockfile",
  "buildCommand": "cd /workspace/projects && next build",
  "devCommand": "cd /workspace/projects && pnpm dev",
  "framework": "nextjs"
}
